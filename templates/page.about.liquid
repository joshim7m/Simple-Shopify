{% render 'page-header' %}

  <section class="about section">
    <div class="container">
      <div class="row">
        <div class="col-md-6">
          <img class="img-fluid" src="{{ settings.about_image | img_url: '1620x' }}">
        </div>
        <div class="col-md-6">
          {{ page.content }}
        </div>
      </div>
    </div>
  </section>